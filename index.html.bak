<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>CS2 Demo Downloader</title>
    <meta http-equiv="Content-Security-Policy" content="script-src 'self' 'unsafe-inline';">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom scrollbar for a better look */
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: #1f2937; }
        ::-webkit-scrollbar-thumb { background: #4b5563; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #6b7280; }
    </style>
</head>
<body class="bg-gray-900 text-gray-200 font-sans antialiased">

    <div class="container mx-auto p-6 max-w-3xl">
        <header class="flex items-center space-x-4 mb-8">
            <!-- Simple App Icon -->
            <svg class="w-16 h-16 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path></svg>
            <div>
                <h1 class="text-3xl font-bold text-white">CS2 Demo Downloader</h1>
                <p class="text-gray-400">A simple GUI to download and decompress match demos.</p>
            </div>
        </header>

        <main class="bg-gray-800/50 backdrop-blur-sm rounded-lg shadow-2xl p-6 border border-gray-700 space-y-6">
            <!-- Share Code Input -->
            <div>
                <label for="share-code" class="block text-sm font-medium text-gray-300 mb-2">Share Code or Steam Link</label>
                <div class="relative">
                    <input type="text" id="share-code" placeholder="CSGO-xxxx-xxxx..." class="w-full bg-gray-700 border border-gray-600 rounded-md px-4 py-2.5 text-white focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-200">
                </div>
            </div>

            <!-- Download Folder Picker -->
            <div>
                <label for="download-folder" class="block text-sm font-medium text-gray-300 mb-2">Download Folder</label>
                <div class="flex items-center space-x-3">
                    <input type="text" id="download-folder" placeholder="No folder selected" readonly class="w-full bg-gray-700 border border-gray-600 rounded-md px-4 py-2.5 text-gray-400 cursor-not-allowed">
                    <button id="select-folder-btn" class="bg-gray-600 hover:bg-gray-500 text-white font-bold py-2.5 px-5 rounded-md transition duration-300 whitespace-nowrap focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-800 focus:ring-gray-500">
                        Select...
                    </button>
                </div>
            </div>

            <!-- Download Button -->
            <div class="pt-2">
                <button id="download-btn" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-4 rounded-md transition duration-300 disabled:bg-gray-600 disabled:cursor-wait flex items-center justify-center space-x-2 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-800 focus:ring-blue-500">
                    <svg id="download-icon" class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path></svg>
                    <span id="download-btn-text">Download Demo</span>
                </button>
            </div>
        </main>

        <!-- Status/Log Area -->
        <footer class="mt-8">
            <h2 class="text-lg font-semibold text-gray-200 mb-3">Status Log</h2>
            <div id="status-container" class="bg-gray-900/80 rounded-lg p-4 h-48 overflow-y-auto font-mono text-sm text-gray-300 space-y-2 border border-gray-700">
                <p class="text-gray-500">Waiting for input...</p>
            </div>
        </footer>
    </div>

    <script src="./renderer.js"></script>
</body>
</html>
